<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <EditText
                android:id="@+id/rule_replace_pattern"
                style="@style/Rule_EditTextMultiline"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:hint="@string/rule_replace_pattern" />

            <CheckBox
                android:id="@+id/rule_replace_regex"
                android:layout_width="wrap_content"
                android:layout_height="@dimen/rule_item_height"
                android:text="@string/rule_regex" />

        </LinearLayout>

        <EditText
            android:id="@+id/rule_replace_replacement"
            style="@style/Rule_EditTextMultiline"
            android:hint="@string/rule_replace_replacement" />

        <include layout="@layout/rule_section_applyto" />

    </LinearLayout>

</ScrollView>